<div id="person-list">
<h2>Searching...</h2>

Here are the results for your query "<%= @query %>"

<table>
  <tr class="list-line-header">
    <th>Picture</th>
    <th>First name</th>
    <th>Last name</th>
    <th>Email</th>
    <th></th>
  </tr>

<% @people.each do |person| %>
  <tr class="<%= cycle('list-line-odd', 'list-line-even') %>">
    <td><%= image_tag person_path(person, :jpg) %></td>
    <td><%= highlight(person.first_name, @query) %></td>
    <td><%= highlight(person.last_name, @query) %></td>
    <td><%= highlight(person.email, @query) %></td>
    <td class="list-actions">
    <%= link_to "Show more details", :action => "show_details", :id => person %>
    </td>
  </tr>
<% end %>
</table>

</div>

<div id="person-list">
<h2>Manage people</h2>

<div id="commands">
<%= link_to 'New person', new_person_path %>
</div>


<table>
  <tr class="list-line-header">
    <th>First name</th>
    <th>Last name</th>
    <th>Email</th>
    <th>Phone1</th>
    <th>Phone2</th>
    <th>Skype</th>
    <th>Address</th>
    <th>Image url</th>
  </tr>

<% @people.each do |person| %>
  <tr class="<%= cycle('list-line-odd', 'list-line-even') %>">
    
    <td><%=h person.first_name %></td>
    <td><%=h person.last_name %></td>
    <td><%=h person.email %></td>
    <td><%=h person.phone1 %></td>
    <td><%=h person.phone2 %></td>
    <td><%=h person.skype_id %></td>
    <td><%=h person.address %></td>
    <td><%= image_tag person_url(person, :jpg)  %></td>
    <td class="list-actions">
    	<%= link_to 'Show', person %><br/>
    	<%= link_to 'Edit', edit_person_path(person) %><br/>
    	<%= link_to 'Destroy', person, :confirm => 'Are you sure?', :method => :delete %>
    </td>
  </tr>
<% end %>
</table>

</div>